## Why

<!-- What problem does this solve? Spend more words here than on "what changed".
     If it comes from an API constraint, cite the constraint.
     If it ports a change from the TypeScript original, name it (TS #13) and say
     what the JVM changed about it. -->

## Trade-offs

<!-- What alternatives were considered, and what was given up?
     "None" is a valid answer, but say it explicitly. -->

## How it was tested

<!-- Automated, manual, or "not tested" — just be honest about which. -->

## Checklist

- [ ] `./gradlew check` passes
- [ ] Does not violate any constraint in `CLAUDE.md`
- [ ] Does not call any order-placing endpoint
- [ ] Behaviour matches the original, or the difference is stated above
