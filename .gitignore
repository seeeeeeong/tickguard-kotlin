.gradle/
build/
.kotlin/
.idea/
*.iml
local.properties
.DS_Store
*.log

# Credentials for a live brokerage account. Never committed.
.env

# sqlite
*.db
# A database the original service wrote, with made-up data: the cut-over test reads it.
!adapters/src/test/resources/ts-made.db
*.db-wal
*.db-shm

# Design notes are kept locally, out of the repository.
docs/

# JVM heap dumps, written to the working directory on OutOfMemoryError.
*.hprof
